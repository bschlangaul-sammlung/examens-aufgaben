\documentclass{lehramt-informatik-aufgabe}
\liLadePakete{formale-sprachen}
\begin{document}
\liAufgabenTitel{p Zeichen a und aus q Zeichen b }
\section{Aufgabe 8
\index{Turing-Maschine}
\footcite{66115:2010:09}}

Konstruieren Sie eine Turingmaschine $M$ mit $L(M) = L$, wobei $p, q
\geq 1$. Beschreiben Sie zusätzlich, wie $M$ arbeitet (Stil: $M$ liest
das Zeichen $a$ und speichert ....)

\liAusdruck{w}{w \in \liMenge{a, b}*, w\text{ besteht aus }p\text{ Zeichen }a\text{ und aus }q\text{ Zeichen }b }.

\end{document}
