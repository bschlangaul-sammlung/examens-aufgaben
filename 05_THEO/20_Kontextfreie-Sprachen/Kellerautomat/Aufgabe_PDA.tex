\documentclass{lehramt-informatik-aufgabe}
\liLadePakete{automaten,mathe}
\begin{document}
\let\m=\liMengeOhneMathe
\let\u=\liKellerUebergang
\def\z#1{$z_#1$}

\liAufgabenTitel{PDA}
\section{PDA
\index{Kellerautomat}
\footcite[Aufgabe 7: PDA aufstellen]{theo:ab:2}}

Erstelle jeweils einen PDA, der die angegebenen Sprachen akzeptiert.

\begin{enumerate}

%%
% (a)
%%

\item $L = \{0^n 1^n \, | \, n \in \mathbb{N} \}$

\begin{liAntwort}
N = Null

\begin{center}
\begin{tikzpicture}[li kellerautomat,node distance=1.5cm]
\node[state,initial] (0) {\z0};
\node[state,above right=of 0] (1) {\z1};
\node[state,right=of 1] (2) {\z2};
\node[state,below right=of 2] (3) {\z3};

\path (0) edge[above left] node{\u{0 raute Nraute}} (1);
\path (1) edge[above,loop] node{\u{0 N NN}} (1);
\path (1) edge[above] node{\u{1 N epsilon}} (2);
\path (2) edge[above,loop] node{\u{1 N epsilon}} (2);
\path (2) edge[above right] node{\u{epsilon raute epsilon}} (3);
\end{tikzpicture}
\end{center}
\liFussnoteUrl{https://flaci.com/Aji1r8mf7}
\end{liAntwort}

%%
% (b)
%%

\item Alle Wörter, die gleich viele $a$ wie $b$ enthalten.

\begin{liAntwort}

\begin{center}
\begin{tikzpicture}[li kellerautomat,node distance=4cm]
\node[state,initial] (0) {\z0};
\node[state,above right=of 0] (1) {\z1};
\node[state,below right=of 0] (2) {\z2};
\node[state,above=of 0] (3) {\z3};

\path (0) edge[right,bend left] node{\u{
  a raute Araute,
}} (1);

\path (0) edge[right,bend left] node{\u{
  b raute Braute,
}} (2);

\path (0) edge[left] node{\u{
  raute epsilon epsilon,
}} (3);

\path (1) edge[right,bend left] node{\u{
  b A epsilon,
}} (0);

\path (1) edge[above,loop right] node{\u{
  b A epsilon,
  a A AA,
}} (1);

\path (2) edge[above,loop right] node{\u{
  a B epsilon,
  b B BB,
}} (2);

\path (2) edge[left,bend left] node{\u{
  a B epsilon,
}} (0);
\end{tikzpicture}
\end{center}

\liFussnoteUrl{https://flaci.com/Arar6fos7}
\end{liAntwort}

%%
% (c)
%%

\item Alle Wörter, bei denen kein Präfix mehr Einsen wie Nullen hat.

\begin{liAntwort}
akzeptiert

01

00101

nicht akzeptiert:

1 11
\end{liAntwort}
\end{enumerate}
\end{document}
